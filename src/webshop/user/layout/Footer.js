import { Layout } from 'antd';
import React from 'react';
const {Footer} = Layout;
const FooterComponent = () => {
    return(
        <Footer style={{ textAlign: 'center' }}>Bản quyền thuộc về MinaShop</Footer>
    )
}
export default FooterComponent;